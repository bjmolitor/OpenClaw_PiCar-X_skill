[project]
name = "openclaw-picarx-skill"
version = "0.1.0"
description = "Agent-friendly CLI for SunFounder PiCar-X v2.0 (OpenClaw skill MVP)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-2.0-or-later" }

[project.scripts]
aiagentctrl = "aiagentctrl:main"

[tool.ruff]
line-length = 100
